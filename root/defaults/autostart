#!/bin/bash
set -e

export DISPLAY=${DISPLAY:-:1}

for i in $(seq 1 20); do
    if xdpyinfo -display "$DISPLAY" >/dev/null 2>&1; then
        break
    fi
    sleep 1
done

cd /EDDiscovery
exec mono --debug EDDiscovery.exe